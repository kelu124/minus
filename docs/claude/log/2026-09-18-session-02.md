# Session 02 — 2026-09-18

## Workshop activities

- User framed *minus* as a **pulse-echo device for a workshop** and asked to start an
  `activities/` folder with a `possibilities.md` listing what workshop participants
  could do — spanning **hardware, ultrasound, embedded firmware, and computer side**.
- Created `activities/possibilities.md`: a living, difficulty-tagged (🟢 intro /
  🟡 core / 🔴 deep) menu of candidate activities across the four layers, grounded in
  the locked design (RP2350 + external ADC, unipolar pulser, 3–4 MHz piezo, A-/M-mode,
  coded excitation, on-device DSP, RGB LED, I²C OLED, 40-pin header — see
  `memory/0005-minus-direction.md`).
- Each activity notes what it teaches and what board feature / prior activity it needs.
  Added cross-cutting formats (golden path, station format, challenge, take-home kit)
  and open questions for the workshop design (session length, pre-assembled vs soldered,
  raw-RF access gating, phantoms, host tooling standard).

## Anti-requirements

- User asked to add "things we wouldn't want" to `requirements.md`, with the example
  that **open firmware toolchains constrain us to certain MCUs / FPGA brands**.
- Added **§19 — Anti-requirements (N1–N11)**: stated as negatives / hard filters on
  part & tool choice. N1 captures the example directly (open toolchain ⇒ RP2350 pico-sdk;
  if FPGA ever, open flow only → Lattice iCE40/ECP5 via Yosys+nextpnr, as `we_gate` uses).
  Others: no un-reproducible/single-source parts, no proprietary-EDA lock-in, no
  vendor/OS-locked host or drivers, UF2-only reflash, no black-box data path, no cloud
  dependence, no scope creep past the single-channel badge, no clinical positioning, no
  dangerous/exotic HV, no proprietary connector lock-in. Added a pointer from §1
  out-of-scope.

## PIC32A + RP2350 concept

- Owner raised: pair a **Microchip PIC32AK3208GC41064 (PIC32A)** with the RP2350 —
  PIC32A op-amps as the gain stage, its ADC to capture ~150 µs A-lines, transfer to
  RP2350 which preprocesses + exposes over USB; RP2350 could even flash the PIC.
- Verified the part from the family product brief (DS70005582, mirrored to
  `pdfs/datasheets/`): **2× 12-bit ADC up to 40 Msps**, **3× 100 MHz rail-to-rail
  op-amps** (100 V/µs), 3 comparators + 12-bit DACs, HS-PWM (2.5 ns) w/ ADC trigger,
  PTG, 3.0–3.6 V, 200 MHz FPU/DSP core, ~32 KB flash / ~16 KB SRAM, 64-pin, **no USB**.
- Wrote `design/pic32/README.md` as a trade study. Key finds:
  - **Relevant:** the on-die 40 Msps/12-bit ADC + 100 MHz op-amps clear P2/P3/P4 with
    margin and **collapse the external ADC (ADEC) + external VGA into one chip** —
    also removes the hardest baseline task (PIO-clocked external-ADC capture). 150 µs
    fits SRAM (6 KB @20 Msps / 12 KB @40 Msps).
  - **No USB on the PIC** is the clean justification for keeping the RP2350 as the
    open USB front-end + DSP + UF2/ICSP programmer (one USB-C port boots both).
  - **Main tension: anti-req N1** — PIC32A needs Microchip MPLAB/XC (closed) toolchain,
    unlike RP2350 pico-sdk. Mitigation: ship a prebuilt PIC image the RP2350 flashes;
    only PIC-firmware hackers need the closed tools. Also: fixed op-amps ≠ true VGA
    (TGC needs a scheme), second-MCU firmware complexity, DP4/DP5 tension, and LCSC
    stock/price TBD (N2).
- Left an open-questions checklist in the note (LCSC price, TGC approach, TX sequencer
  ownership, inter-MCU SPI protocol, ICSP-from-RP2350, net BOM vs baseline).

## PIC32A follow-ups: price, gain stage, TGC dropped

- **Price/availability check.** Digikey lists `PIC32AK3208GC41064-I/PT` (TQFP-64,
  32 KB flash, **8 KB RAM**) at **$1.91/1, $1.73/25, $1.58/100**, 498 in stock, 7-wk
  factory lead. So the PIC32A replaces the external **ADC + VGA for < $2** — a strong
  DP2 win, cheaper than the baseline ~$16 active-IC set. **Caveat:** Digikey ≠ LCSC;
  **LCSC/JLCPCB availability still unconfirmed** (N2) — must verify before locking.
- **Corrected SRAM to 8 KB** (was assuming ~16 KB). Redid capture math: 150 µs fits at
  20 Msps (6 KB) but **not** at 40 Msps (12 KB) → PIC grabs one line ≤8 KB and bursts
  out; RP2350 buffers longer/multiple lines.
- **Owner decision: TGC not needed.** Gain doesn't vary within a line; the user just
  **sets a fixed gain and can change it between firing lines**. Big simplification
  (no fast DAC ramp, no gain/depth timing sync).
- **Gain-stage design (new §3b in `design/pic32/`).** PIC32A op-amp gain is set by
  external feedback R (no confirmed internal PGA ladder — verify). To make it settable
  between lines: **digipot/MDAC in feedback** (recommended, ~$0.5–1, = the owner's "DAC
  controls gain" done right — gain lives in a resistor ratio, so use a digital resistor,
  not a DAC voltage), or a **resistor-mux** (cheapest, stepped), or an external VGA/PGA.
  Owner's **LNA-first** idea captured as optional A3a: a fixed low-noise front stage
  ahead of the settable gain, for weak 5 V-pulser echoes.
- **requirements.md updated:** F6 → [M] per-line settable gain (TGC explicitly not
  required); A3 rewritten; A3a (optional LNA) added; P8/A1/§17 de-TGC'd.

## New rule: Markdown sibling for every PDF

- Owner: keep a Markdown version next to each PDF (markitdown installed locally).
- Added the rule to **CLAUDE.md**, **`pdfs/README.md`**, and the **minus-docs skill**
  (`markitdown foo.pdf -o foo.md`; commit the `.md`; skip purely graphical schematics).
- Generated `.md` for all 11 PDFs (`pdfs/` + `pdfs/datasheets/`). Largest =
  `rp2350-datasheet.md` (61,890 lines / 2.9 MB, well under the 45 MB cap).

## PIC32A chip choice + RP2350 interconnect (new §3c, §3d)

- **§3c chip choice.** Family = **36 / 48 / 64-pin** (VQFN/TQFP), flash/RAM tiers
  **32 KB/8 KB (`3208`) … 128 KB/16 KB (`1216`)**; pins and memory are independent axes.
  Pin-budget estimate (~12–16 pins) → **48-pin is the safe sweet spot**, 36-pin
  borderline, 64-pin generous (unneeded since RP2350 owns the 40-pin header).
  **Program memory:** firmware is small → **flash ~32 KB is ample**; the real lever is
  **SRAM** (8 KB → 150 µs @20 Msps; 16 KB → 150 µs @40 Msps). Recommend starting with
  `PIC32AK3208GC41048`.
- **§3d interconnect.** RP2350 = master/programmer, PIC = capture engine. Signals:
  **ICSP** (`MCLR`/`PGC`/`PGD`, LVP per DS60001145; `MCLR` doubles as reset) so one
  USB-C port programs both; **SPI** (SCK/MOSI/MISO/CS) for the A-line burst; **RDY** IRQ
  (PIC→RP2350); **TRIG** (RP2350→PIC ADC ext-trigger for TX/RX sync); optional **UART**
  debug/log; shared **3V3**. Minimal set = 9 lines; ICSP pins can be reused at run time.

## PIC32A availability + footprint

- **LCSC/JLCPCB check (2026-09-18): not found.** LCSC site search + JLCPCB parts
  library surfaced nothing for `PIC32AK`; RS/Mouser list it but mostly OOS/2026 lead;
  Digikey has it in stock. New part (2024). ⇒ for the LCSC/JLCPCB flow it'd be a
  consigned/"extended" part (feeder fee) or Digikey + hand-place. **Main sourcing risk**
  — recorded in `design/pic32/` §3a and the open-items list; re-check periodically.
- Found an extra memory tier: **`6416` = 64 KB flash / 16 KB RAM** — the cheapest way to
  16 KB RAM (for 150 µs @ 40 Msps), no need for the 128 KB `1216`.
- **Footprint (owner: keep chips as small as feasible).** Added §3c Footprint guidance:
  bodies ≈ 36-VQFN 5×5, 48-VQFN 6×6 / 48-TQFP 7×7, 64-VQFN 9×9 / 64-TQFP 10×10 mm.
  VQFN is smaller (leadless — N2 caution) but the board already has a QFN RP2350, so
  it's reflow/JLC-assembled anyway → **48-pin VQFN (~6×6 mm)** is the small-but-adequate
  pick (TQFP as hand-solder alt; 36-VQFN only if pin budget confirmed). Also: with the
  PIC doing the ADC/analog, the RP2350 can likely drop from **RP2350B (QFN-80, 10×10)**
  to **RP2350A / RP2354A (QFN-60, 7×7)** — smaller MCU, and RP2354A also deletes the
  external flash chip.

## RP2354A flash + BOOTSEL (from local rp2350-datasheet.md)

- **Confirmed RP2354A needs no external flash:** 2 MB in-package flash (stacked Winbond
  W25Q16JVWI die), QFN-60, pin-identical to RP2350A, 30 GPIO / 4 analog. Caveat:
  QSPI_IOVDD must be 3.3 V. Extra QSPI flash/PSRAM can still be added via a Bank-0 GPIO CS.
- **BOOTSEL button:** QSPI pads (incl. QSPI_CSn/SS) are still bonded out; BOOTSEL =
  QSPI_CSn driven low at reset/power-up. So the button wires **QSPI_CSn → GND via ~1 kΩ**
  (same as a flashless RP2350A/Pico), optionally paired with a **RUN reset button**
  (hold BOOTSEL, pulse RUN) to avoid a power cycle. Captured in `design/pic32/` §3c.

## RX gain-IC survey (LNA + variable gain)

- Owner asked for other LNA+VGA ICs, single→8 channels. Wrote
  **`systems/afe-vga-ics.md`** organised by integration level (VGA-only / LNA+VGA /
  full AFE with ADC / transceiver) and channel count:
  - 1-ch: AD8331, AD8338, AD8337/8336, AD603, VCA810/821/824 (+ cheap digital PGAs).
  - 2-ch: AD8332, VCA2612–2617, LMH6521.
  - 4-ch: AD8334/8335, LMH6522, AFE5401 (+ADC).
  - 8-ch full AFE: AFE5808A/5809/5818, AD9276(12b/80M)/AD9277(14b/50M)/AD9278-79,
    AD9670/9671, MAX2082 (transceiver). VGA-only octal (no ADC): VCA5807/8500/8617,
    MAX2078/79.
  - 16-ch: AFE5832/LP (low-power/POCUS).
  - Relevance: AD8331/AD8338 stay the low-cost single-channel picks; PIC32A path can use
    a VGA-only part or digipot-in-feedback; octal AFEs are over-spec/expensive but the
    multi-channel reference; most AFEs not LCSC-stocked (N2). Verified AD927x/AFE5808
    specs by search.
- Linked from `systems/README.md`, `systems/by-ic.md`, `options.md` §2.
- **Follow-up: LNA pairing for VGA-only parts.** VCA810 (2.4 nV/√Hz) / AD603
  (1.3 nV/√Hz) are too noisy alone → precede with a <1 nV/√Hz fixed LNA (~15–25 dB):
  op-amp LNAs **OPA847 (0.85), LMH6629 (0.69), AD8099 (0.95), AD797 (0.9),
  ADA4898 (0.9)**, or the dedicated dual ultrasound LNA **AD8432** (0.85 nV/√Hz + built-in
  T/R protection). Input protection = diode limiter / MD0100 / built-in clamp. Reality:
  most reference designs use an **integrated-LNA VGA/AFE** (AD8331/AD8338/AFE5808) rather
  than discrete LNA+VGA; the discrete route is the DIY/flexible one. Added as a section in
  `systems/afe-vga-ics.md`.

## Gain/LNA IC prices (LCSC, 2026-09-18)

- Method: LCSC product-detail pages are WebFetch-able once you have the `C`-code
  (harvest codes via search). Added a price table to `systems/afe-vga-ics.md` and
  refreshed `options.md` §2.
- Key numbers (qty ~50): **AD8331** C203731 $14.23 (in stock, 48); **AD603-REEL**
  C578331 $8.92 (in stock, 2459); **AD8338-RL** C652715 $7.72@30 (**OOS**);
  **AD8332** C578698 ~$16 (**OOS**); LNAs **OPA847** C160422 $5.06, **LMH6629**
  C206003 $4.98, **ADA4898-1** C207523 $5.06 (all in stock); **AD797** C50658 ~$11.
- **Not LCSC-stocked** (Digikey/Mouser only): **VCA810**, **AD8432**, **AFE5808**,
  **AD9276** (N2 flag for the JLC flow). VCA820 (VCA810 family) is on LCSC but low
  stock (7) / ~$14.
- Take: cheapest in-stock LNA+VGA today ≈ **AD603 (~$9) + a $5 LNA ≈ $14**, on par with
  the integrated **AD8331 (~$14)**; AD8338 (the cheap favourite) is OOS.

## DesignA — cheapest RP2354A + PIC32A build

- Owner asked for the cheapest system on RP2354 + PIC32, detailing pulser + gain.
  Wrote **`design/designA/README.md`**.
- **RP2354A is on LCSC/JLC** (C41378174, ~$1.27, 16k stock) — so only the PIC32A is
  non-JLC. Updated the pic32 note accordingly.
- Architecture: PIC32A owns TX (HS-PWM) + capture (on-die 40 Msps ADC + op-amp gain);
  RP2354A owns USB-C, control, DSP, WS2812, and flashes the PIC over ICSP. 2 MB
  in-package flash → no external flash chip.
- **Pulser:** cheapest U0 — single N-ch logic-level MOSFET, low-side, off USB +5 V,
  driven by PIC HS-PWM (coded excitation), BAV99 T/R clamp, HV node on T7 jumper.
- **Gain (no TGC):** PIC32A op-amp with an **MCP4131 digipot in feedback** → per-line
  settable gain (RP/PIC sets code over SPI); cheaper alt = resistor-mux steps; optional
  LNA (LMH6629/ADA4898-1/OPA847 ~$5) only if SNR limits.
- **BOM:** active ICs ≈ **$3.74**, board ≈ **$5.2** (ex-piezo) vs ~$33 baseline. Only
  N2 caveat: PIC32A not LCSC-stocked (consigned/Digikey).
- Open: PIC32A sourcing, op-amp noise/LNA need, digipot BW at 3–4 MHz, pulse edges,
  48-pin budget.

## DesignA: PIC32 flashing, drive-select jumper, mermaid

- **Flash the PIC32 directly (owner req):** added **S4a** to requirements — expose the
  PIC32 **ICSP header** (5-pin Microchip: MCLR/VDD/GND/PGD/PGC) for direct **PICkit/MPLAB
  Snap** flashing (provided `.hex` via free MPLAB IPE; building needs closed XC-DSC, N1).
  Wrote **`design/pic32/pic32.md`** covering how PIC32(A) is programmed: ICSP 2-wire, the
  three PGEC/PGED pairs + JTAG, tools, **LVP** (why RP2354 can bit-bang it), MCLR network,
  and the two flashing paths sharing one PGC/PGD/MCLR net (J3 + RP2354).
- **Pulser drive-select (owner req):** added **T4a** — the N-FET gate is drivable by
  **either PIC32 HS-PWM or RP2354 PIO**, chosen by **JP1** (3-pin, gate=centre); only one
  drives, the other Hi-Z. Updated DesignA §4 schematic + BOM (JP1, J3).
- **Mermaid** diagrams added to DesignA: system block, programming paths, and a
  one-A-line acquisition sequence diagram.

## DesignA toolchain + pic32arick reuse

- **Toolchain doc** `design/designA/toolchain_designA.md` (Ubuntu, CLI + Makefile):
  RP2354 via **pico-sdk/CMake → uf2** (+ picotool); PIC32A via **XC-DSC → hex**
  (`make` on the MPLAB-X-generated makefiles) + **`ipecmd`/PICkit** (or `mdb`) flashing;
  top-level Makefile (`make` / `make flash-rp` / `make flash-pic`). Confirmed by search:
  **XC-DSC officially supports Linux + the PIC32A family** (v3.x) — so fully headless.
  One-USB field path = RP2354 UF2 then bit-bang ICSP to the PIC.
- **pic32arick reuse (owner pointed to it):** [github.com/kelu124/pic32arick] is the
  owner's PIC32AK1216GC41064 board (docs-only). Folded its validated blocks into DesignA:
  - **Pulser:** 5 V **push-pull** IRLML6244(N)/IRLML2244(P) + **TC4427A** dual gate driver
    (3V3→5V, dead-band), ferrite-isolated 5 V rail, Rdamp 100–200Ω, active-damping option.
    Single-N-FET kept as the absolute-minimum variant. JP1 drive-select feeds the driver.
  - **Gain:** 3 on-die op-amps — OA1 fixed +14.5 dB, OA2 variable via **MCP4531-103 I²C
    digipot** (6–42 dB), OA3 mid-rail bias; combined +20.5…+56.5 dB; 2-pole RC AAF → ADC.
    (Switched digipot from MCP4131/SPI to MCP4531/I²C to match pic32arick.)
  - **T/R:** **MD0100** switch (BAV99 = cheaper fallback).
  - **ICSP header:** 6-pin (MCLR/VDD/GND/PGED1/PGEC1/NC), **47 Ω** series on PGC/PGD,
    **Tag-Connect TC2030-IDC-NL**, and **off-center/alternated friction-fit holes** for a
    solderless press-fit (per Microchip `pic32ak1216gc41064-gpdim-demo`).
- BOM updated: ~**$4.7** active ICs / ~**$6.3** board; min single-N-FET variant ~$5.4.
- pic32arick added to `docs/claude/memory/0004-key-references.md`.

## Open / next

- Confirm the default "golden path" track once session length + audience are known.
- Resolve RF-ACCESS decision (affects which firmware/host activities are unlocked).
