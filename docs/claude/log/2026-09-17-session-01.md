# Session 01 — 2026-09-17

## Kickoff

- User introduced the project: building **minus**, a minimal single-channel,
  cheap ultrasound imaging platform. Questions to follow across sessions.
- User asked to set up the repo so that all discussions are logged, with a
  `.claude/` folder and a `docs/claude/memory/` folder to persist the exchange.

## Actions taken

- Created `docs/claude/` structure:
  - `README.md` — logging & memory conventions.
  - `log/` — chronological session logs (this file).
  - `memory/` + `INDEX.md` — durable decisions/facts.
  - `memory/0001-project-scope.md` — project scope + open questions.
  - `memory/0002-working-conventions.md` — how we log & persist memory.
- Created `.claude/` with a README pointing at the convention.

## Update — documentation rules extended

- User asked to also keep `docs/claude/TODO.md` and `docs/claude/DONE.md`, and to
  log every commit (at least a summary) in `docs/claude/changelog.md`.
- These are now codified as session rules in the repo root `CLAUDE.md` so a new
  chat comes up already knowing the past discussions/discoveries.
- User also asked for **repo-scoped skills** for documenting the work — usable
  only inside this repo, never outside. Added project skill `minus-docs` under
  `.claude/skills/`.

## Actions taken (added)

- Created `docs/claude/TODO.md`, `DONE.md`, `changelog.md`.
- Rewrote `CLAUDE.md` with start-up + during-session + per-commit rules.
- Updated `docs/claude/README.md` and `memory/0002-working-conventions.md`.
- Created `.claude/skills/minus-docs/SKILL.md` (repo-local skill).

## Update — systems survey

- User pointed to the wulrick "extended platform survey" and asked to create a
  `systems/` folder (subfolder per design), do a first-pass review, and build a
  datasheet **template** at the folder root covering MCU/FPGA, pulser
  (polarity/voltage), acquisition route, ADC, gain/TGC, connection bandwidth,
  wireless, single-channel, etc.
- Fetched the survey + `OtherSystems/*.md` (EchoLite, PuLsE, TinyProbe, USoP,
  WULPUS, WULPUS_PRO, pic0rick_ports) from GitHub raw.
- Built `systems/TEMPLATE.md` (10-section datasheet) + `systems/README.md`
  (index + at-a-glance table) + one populated sheet per system (7 total).
- Recorded findings in `memory/0003-systems-survey.md`.

## Update — survey extended with online research

- User asked to process the template (search online to fill gaps), review other
  non-closed designs, review the literature, and avoid 64+ element designs.
- Web searches (un0rick/lit3rick, open pulse-echo boards, TUSS4470, low-cost
  single-element reviews, open FPGA platforms). Primary specs pulled from
  un0rick.cc + un0rick GitHub README; arXiv 1611.10174 abstract for Murgen.
- Added 4 sheets: Murgen (echOpen, 2016), un0rick (iCE40, 2019), lit3rick (UP5K,
  2021), TUSS4470 (analog-envelope AFE IC route). systems/ now 11 sheets.
- Reframed systems/README into 5 design branches; added "Out of scope (64+
  elements)" — ULA-OP/ULA-OP 256, SARUS, open-UST — and a "Leads to review" list.
- Updated memory/0003 accordingly.
- Note: some fetches were blocked (ResearchGate/ScienceDirect 403); lit3rick exact
  ADC/HV/power and the Measurement-2024 platform's channel count remain to confirm.

## Update — processed user PDFs + new folder conventions

- Answered RP2350B ADC question (500 kSps max, 12-bit SAR; too slow for raw RF →
  external ADC needed). Saved RP2350 datasheet to `pdfs/`.
- User added 3 PDFs to `pdfs/`: IUP (SSRN 6946751, DRESDYN UDV node), MEMS-US
  (Sci. Reports 2020), and the Jonveaux et al. 2022 open-hardware review.
- Read all three. Created `systems/iup/` and `systems/mems-us/` datasheets.
- Built `systems/literature.md` from the review (Table 2 filtered to ≤32 elements;
  component menus for pulsers/HV/TGC/ADC/AFE/mux; single-element B-mode strategies;
  bandwidth-reduction). Updated `systems/README.md` (new rows, leads, references).
- New rule from user: store downloaded PDFs in `pdfs/`, schematics/design files in
  `design/<name>/`. Created both folders (+ READMEs) and encoded the rule in
  `CLAUDE.md`, the `minus-docs` skill, and memory/0002.
- Notable finding: IUP is nearly a *minus*-shaped reference — un0rick's analog chain
  (iCE40 + MD1213/TC6320 + AD8331 + MD0100 + DAC) + STM32H7 + SD + Wi-Fi + 16-bit ADC.

## Update — design files, SIG-WUS, Weik review

- User: don't gitignore PDFs; pull design files from repos where possible; process
  the SIG-WUS OXP page; later, process the Weik et al. 2026 wearables review PDF.
- Pulled design files from kelu124 GitHub into `design/{un0rick,lit3rick,pic0rick}/`
  (schematic PDFs, KiCad sources, gerbers, BOMs) + `SOURCE.md` w/ commit SHAs;
  cross-linked from the datasheets. pic0rick's 3-in-1 adc+pulser+hv panel is the key
  single-board front-end reference. pic0rick article → `pdfs/pic0rick_full.pdf`.
- SIG-WUS: OXP catalog site's live data failed to load (JS); its peer-reviewed basis
  is the Weik review, which the user then supplied.
- Read the Weik et al. 2026 review (IEEE RBME). Captured its Table I in
  `systems/literature.md`; added datasheets SENS-U, WMAUS (+Yin variant note),
  MoUsE, Flopatch. systems/ now 17 sheets. Added memory 0004 (key references).

## Update — SIG-WUS catalog data, FloPatch, 45 MB cap

- User: don't push files >45 MB (repo total size fine); get more FloPatch info;
  retry SIG-WUS via its repo data file; commit all + push to remote.
- Retrieved the SIG-WUS OXP catalog from `github.com/sig-wus/sig-wus-oxp.github.io`
  `platforms/*/index.json` (live site JS-catalog was broken). Snapshot →
  `systems/_sig-wus-oxp/` (14 platforms + reconciliation table).
- FloPatch: enriched to model FP120, CW 4 MHz, FDA-cleared K200337 (2020) + CE,
  Flosonics Medical (Toronto), iOS app (velocity/VTI/ccFT), Kenny et al. 2021.
- Corrected SENS-U → TENA SmartCare (Essity), 2021, pediatric.
- Added BioGAP WULPUS-pro sheet (ETH open shield `pulp-bio/sensei-us-shield`);
  cross-ref from wulpus-pro. systems/ now 18 sheets.
- Encoded the 45 MB per-file cap in CLAUDE.md, minus-docs skill, memory 0002.
- Remote confirmed: origin = git@github.com:kelu124/minus.git. Committing + pushing.

## Update — BioGAP design files, by-IC, Open Echo, opensourceimaging

- User: push to main; pull BioGAP design files; identify MSP430FR5043-based and
  TUSS4470-based designs; also check opensourceimaging.org/projects.
- Pulled `design/biogap-wulpus-pro/` (schematics/assembly PDF + BOM, Altium repo)
  and `design/open-echo/` (KiCad + gerbers + BOM).
- Added `systems/by-ic.md`: MSP430FR5043 designs (WULPUS/WULPUS PRO/BioGAP + EMG-US
  fusion + TI EVM430-FR6043/FR5043) and TUSS4470 designs (**Open Echo** Neumi/
  open_echo + TI BOOSTXL EVM). Plus an "other recurring parts" index.
- Added the **Open Echo** datasheet (open TUSS4470 Arduino-shield sonar; the cheapest
  realized TUSS4470 route). systems/ now 19 sheets.
- opensourceimaging.org/projects: only un0rick + echOpen (=Murgen), already covered.
- Committing + pushing to origin/main.

## Update — per-system attributes + first analysis

- User: add "works with 1–5 MHz piezo" + "sampling speed" attributes per system;
  then write root `analysis.md` (integrated vs external-ADC routes) with light-BOM
  pulser options; pull IC datasheets to `pdfs/datasheets/` if needed.
- Added both attributes as a callout under §1 of all 19 sheets; added them to
  `TEMPLATE.md`; added a consolidated quick-ref table to `systems/README.md`.
- Wrote `analysis.md`: Route A (MSP430, ≤3 MHz) / A2 (TUSS4470, ≤1 MHz) vs Route B
  (external ADC, 1–5 MHz ✓); recommends Route B for a 1–5 MHz minus. Light-BOM
  pulser table: TUSS4470 / STHV748 (1 IC + T/R) / MD1213+TC6320 (proven) / unipolar
  MOSFET+boost / TC6320+driver.
- Pulled datasheets → `pdfs/datasheets/` (MD1213, MD1213DB1 = MD1213+TC6320 ref,
  AN-H53, TUSS4470); STHV748 + TC6320 hosts blocked → linked in the index.
- Added memory 0005 (provisional direction).

## Update — requirements draft

- User asked for a requirements document. Wrote root `requirements.md` (v0.1):
  MoSCoW-prioritised requirements (functional, performance, AFE, pulser, data,
  control, power, mechanical, cost, SW, openness, safety) + verification/acceptance
  + traceability to the survey/analysis. §4 lists open decisions TBD-1..7 (assumed:
  non-clinical, full 1–5 MHz, bipolar-preferred, USB baseline, A-mode core, ≤$150,
  controller open). Cross-linked from analysis.md; memory 0005 updated.

## Next

- Resolve requirements TBD-1..7 → v1.0 → costed BOM + block diagram.
- Sheet remaining leads (Measurement-2024, rtl-ultrasound, Kruizinga).
